{
  "name": "basic_platformer_test",
  "version": "1.0.0",
  "description": "",
  "source": "src/index.html",
  "scripts": {
    "clean": "rimraf dist/",
    "copy-files": "copyfiles -u 1 src/**/*.css src/assets/* dist/",
    "build": "npm run clean && npm run copy-files && parcel build",
    "start": "npm run clean && npm run copy-files && parcel"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "parcel": "^2.8.3",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "kaboom": "^3000.0.0-beta.3"
  }
}
